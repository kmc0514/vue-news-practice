export default {
    fetchedNews(state) {
        return state.news;
    },
    fetchedAsk(state) {
        return state.asks;
    },
    fetchedJobs(state) {
        return state.jobs;
    },
    fetchedUser(state) {
        return state.user;
    },
    fetchedComment(state) {
        return state.comment;
    }
}