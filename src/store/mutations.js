export default {
    SET_LIST(state, list) {
        state.list = list;
    },
    SET_USER(state, user) {
        state.user = user;
    },
    SET_COMMENT(state, comment) {
        state.comment = comment;
    }
}